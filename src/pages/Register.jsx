import RegisterComponent from "../components/RegisterComponent"; // Importing the RegisterComponent which handles user registration.

const Register = () => {
  return (
    <div>
      <RegisterComponent />
      {/* Rendering the RegisterComponent that will handle user registration */}
    </div>
  );
};

export default Register; // Exporting the Register component for use in other parts of the application.
